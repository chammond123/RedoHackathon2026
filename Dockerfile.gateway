# ── Node.js API Gateway Container ─────────────────────────
FROM node:22-slim AS gateway

WORKDIR /app

COPY gateway/package.json gateway/package-lock.json* ./
RUN npm install --production

COPY gateway/src/ src/

EXPOSE 3001

CMD ["node", "src/index.js"]
